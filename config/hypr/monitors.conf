# Virtual headless output — always-on rendering target for remote access (Sunshine/wayvnc)
# Lets Hyprland render to something when lid is closed and eDP-1 is off
monitor=HEADLESS-1,1920x1080@60,auto,1
