# adapter_name must be set per-machine for hybrid GPU laptops (e.g. /dev/dri/renderD129 for Intel iGPU on this ASUS).
# Leave blank for single-GPU desktops — Sunshine will auto-detect.
# adapter_name = /dev/dri/renderD129
capture = kms
key_rightalt_to_key_win = enabled
