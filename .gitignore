# Generated by matugen on every wallpaper change — machine-specific, not tracked
config/waybar/colors.css
config/waybar/colors-dark.css
config/kitty/colors.conf
config/rofi/colors.rasi
config/rofi/colors-dark.rasi

# Quickshell usage tracking — builds up per machine over time
# (stored at ~/.local/share/qs-launcher/usage.json, not in repo)

# OS / editor noise
.DS_Store
*.swp
*.swo
*~
