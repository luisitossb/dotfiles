# ── Matugen-generated color files ────────────────────────────────────────────
# These are regenerated on every wallpaper change. Never commit them.
config/waybar/colors.css
config/waybar/colors-dark.css
config/kitty/colors.conf
config/kitty/colors-matugen.conf
config/rofi/colors.rasi
config/rofi/colors-dark.rasi
config/btop/themes/matugen.theme
config/swaync/colors.css
config/ohmyposh/colors.json
config/quickshell/overview/common/Appearance.colors.qml

# ── Per-machine runtime data ──────────────────────────────────────────────────
# App launcher frecency (builds up per machine, not shared)
# Stored at ~/.local/share/qs-launcher/usage.json — not in repo

# ── OS / editor noise ─────────────────────────────────────────────────────────
.DS_Store
*.swp
*.swo
*~
